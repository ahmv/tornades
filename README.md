# Tornades de Villeray

React project using Yarn for dependency management.

## Available Scripts

Install dependencies:

```bash
yarn install
```

Start the development server:

```bash
yarn start
```

Run tests:

```bash
yarn test
```

Create a production build:

```bash
yarn build
```
